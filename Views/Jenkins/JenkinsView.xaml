<UserControl x:Class="DQEHelper.Views.Jenkins.JenkinsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent" Padding="15">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- КОЛОНКА 1: ГЛОБАЛЬНЫЕ ПАРАМЕТРЫ -->
        <Border Grid.Column="0" Background="#FAFAFA" CornerRadius="10" Padding="15" Margin="0,0,10,0" BorderBrush="#DDDDDD" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="ПАРАМЕТРЫ СБОРКИ" FontWeight="Bold" FontSize="14" Foreground="#10288C" Margin="0,0,0,15"/>

                <TextBlock Text="Destination:" FontWeight="SemiBold" Foreground="#555"/>
                <ComboBox x:Name="DestinationCombo" Height="30" Margin="0,0,0,10" SelectedIndex="0">
                    <ComboBoxItem Content="prod-il"/>
                    <ComboBoxItem Content="prod-eu"/>
                    <ComboBoxItem Content="staging"/>
                </ComboBox>

                <TextBlock Text="Type Report:" FontWeight="SemiBold" Foreground="#555"/>
                <ComboBox x:Name="TypeReportCombo" Height="30" Margin="0,0,0,20" SelectedIndex="0">
                    <ComboBoxItem Content="Main"/>
                    <ComboBoxItem Content="Secondary"/>
                </ComboBox>

            </StackPanel>
        </Border>

        <!-- КОЛОНКА 2: МАТРИЦА ПРОВАЙДЕРОВ -->
        <Border Grid.Column="1" Background="#FAFAFA" CornerRadius="10" Padding="15" Margin="0,0,10,0" BorderBrush="#DDDDDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="ВЫБОР ПРОВАЙДЕРОВ" FontWeight="Bold" FontSize="14" Foreground="#10288C" Margin="0,0,0,10"/>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Выбрать все" Width="90" Height="25" Margin="0,0,10,0" Click="SelectAll_Click"/>
                    <Button Content="Снять все" Width="90" Height="25" Click="DeselectAll_Click"/>
                </StackPanel>

                <!-- Список чекбоксов с привязкой данных -->
                <ListBox x:Name="ProvidersListBox" Grid.Row="2" BorderThickness="1" BorderBrush="#E0E0E0" Background="White" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="5"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- КОЛОНКА 3: ЗАПУСК И ЛОГИ -->
        <Border Grid.Column="2" Background="#FFFFFF" CornerRadius="10" Padding="15" BorderBrush="#DDDDDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button x:Name="RunJobsButton" Content="ЗАПУСТИТЬ ВЫБРАННЫЕ (0)" Height="45" Background="#4CAF50" Foreground="White" FontWeight="Bold" FontSize="14" Cursor="Hand" Click="RunJobsButton_Click">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                </Button>

                <TextBox x:Name="ConsoleLogTextBox" Grid.Row="1" Margin="0,15,0,0" IsReadOnly="True" TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13" 
                         Background="#1E1E1E" Foreground="#D4D4D4" Padding="10"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>