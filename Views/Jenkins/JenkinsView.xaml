<UserControl x:Class="DQEHelper.Views.Jenkins.JenkinsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:models="clr-namespace:DQEHelper.Models"
             Background="Transparent" Padding="15"
             Loaded="UserControl_Loaded">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Левая колонка -->
            <ColumnDefinition Width="380"/>
            <!-- Правая колонка -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ПАНЕЛЬ УПРАВЛЕНИЯ -->
        <Border Grid.Column="0" Background="#FAFAFA" CornerRadius="10" Padding="15" Margin="0,0,15,0" BorderBrush="#DDDDDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- 0: Шапка -->
                    <RowDefinition Height="Auto"/> <!-- 1: Кнопка Парсинг -->
                    <RowDefinition Height="*"/>    <!-- 2: Дерево -->
                    <RowDefinition Height="Auto"/> <!-- 3: Кнопка Экспорт -->
                    <RowDefinition Height="120"/>  <!-- 4: Логи -->
                </Grid.RowDefinitions>

                <!-- ШАПКА -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="ALLURE ПАРСЕР" FontWeight="Bold" FontSize="16" Foreground="#10288C"/>
                        <TextBlock Text="1. Открой отчет Allure в браузере." Foreground="#666" FontSize="12" Margin="0,5,0,0"/>
                        <TextBlock Text="2. Нажми 'Считать ошибки'." Foreground="#666" FontSize="12"/>
                    </StackPanel>

                    <Button Grid.Column="1" Content="🏠 HOME" Height="30" Padding="10,0" Background="#E0E0E0" Foreground="#333" FontWeight="Bold" Cursor="Hand" Click="HomeButton_Click">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                    </Button>
                </Grid>
                
                <!-- ПАРСИНГ -->
                <Button x:Name="ParseAllureButton" Grid.Row="1" Content="🔍 СЧИТАТЬ ОШИБКИ ИЗ ОТЧЕТА" Height="40" Background="#2196F3" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="ParseAllureButton_Click">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                </Button>

                <!-- ДЕРЕВО -->
                <TreeView x:Name="ReportTreeView" Grid.Row="2" BorderThickness="1" BorderBrush="#E0E0E0" Background="White" Margin="0,15,0,15" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type models:ProviderNode}" ItemsSource="{Binding Snaps}">
                            <StackPanel Orientation="Horizontal" Margin="2,4">
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="#10288C" Margin="5,0,0,0"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        
                        <HierarchicalDataTemplate DataType="{x:Type models:SnapNode}" ItemsSource="{Binding Rooms}">
                            <StackPanel Orientation="Horizontal" Margin="2,3">
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="#333" Margin="5,0,0,0"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>

                        <HierarchicalDataTemplate DataType="{x:Type models:RoomNode}" ItemsSource="{Binding Errors}">
                            <StackPanel Orientation="Horizontal" Margin="2,3">
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="#555" Margin="5,0,0,0"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>

                        <DataTemplate DataType="{x:Type models:ErrorNode}">
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Top" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding Title}" TextWrapping="Wrap" MaxWidth="250" Margin="5,0,0,0" Foreground="#555" LineHeight="16"/>
                            </StackPanel>
                        </DataTemplate>
                    </TreeView.Resources>
                </TreeView>

                <!-- ЭКСПОРТ (Вернулся на свое место) -->
                <Button x:Name="ExportCsvButton" Grid.Row="3" Content="💾 ЭКСПОРТ В CSV (БУФЕР ОБМЕНА)" Height="45" Background="#4CAF50" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="ExportCsvButton_Click">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                </Button>

                <!-- ЛОГИ -->
                <TextBox x:Name="ConsoleLogTextBox" Grid.Row="4" Margin="0,15,0,0" IsReadOnly="True" TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" 
                         Background="#1E1E1E" Foreground="#D4D4D4" Padding="8"/>
            </Grid>
        </Border>

        <!-- БРАУЗЕР -->
        <Border Grid.Column="1" BorderBrush="#10288C" BorderThickness="2" CornerRadius="8" ClipToBounds="True">
            <wv2:WebView2 x:Name="JenkinsWebView" />
        </Border>
    </Grid>
</UserControl>