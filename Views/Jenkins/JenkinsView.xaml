<UserControl x:Class="DQEHelper.Views.Jenkins.JenkinsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="Transparent">
    <Grid>
        <Border Background="#FFFFFFFF" CornerRadius="18" Padding="24" Margin="20" BorderBrush="#DDDDDD" BorderThickness="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="760">
                <TextBlock Text="Jenkins API — заглушка" FontSize="28" FontWeight="Bold" Foreground="#10288C" TextAlignment="Center"/>
                <TextBlock Text="Пока функционал Jenkins не реализован. Этот экран служит заглушкой на время разработки." 
                           FontSize="16" Foreground="#444444" TextAlignment="Center" TextWrapping="Wrap"/>
                <TextBlock Text="Позже здесь появится форма подключения к Jenkins, параметры запроса и результаты." 
                           FontSize="14" Foreground="#666666" TextAlignment="Center" TextWrapping="Wrap"/>
                <Button Content="Функция в разработке" Width="240" Height="42" Margin="0,20,0,0" IsEnabled="False"
                        Background="#EAF6AD" Foreground="#10288C" FontWeight="SemiBold" Cursor="Arrow">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
